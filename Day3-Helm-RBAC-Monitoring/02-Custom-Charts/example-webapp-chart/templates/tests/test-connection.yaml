apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-webapp.fullname" . }}-test-connection"
  labels:
    {{- include "example-webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
