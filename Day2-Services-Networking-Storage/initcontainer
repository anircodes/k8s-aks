      initContainers:
      - name: remove-lost-found
        image: busybox:1.36.1 # A lightweight image for simple commands
        command: ["sh", "-c", "rm -rf /var/lib/mysql/lost+found"]
        volumeMounts:
        - name: mysql-persistent-storage-akagalkar
          mountPath: /var/lib/mysql
